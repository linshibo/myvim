#!/bin/bash
pid=$( ps -ef | grep proxy.py | grep -v grep | awk '{print $2}' )
if [  "$pid" != "" ] ; then 
	kill -9 $pid
fi
gnome-terminal --title=goagent --hide-menubar --geometry 60x15 -x python /opt/goagent/local/proxy.py
